# Images

Place your hero background image here as:
  hero-bg.jpg

The image is displayed at its natural pixel size, centered horizontally,
and never scales regardless of screen resolution.

Recommended: use a wide image (e.g. 1920×1080) so it fills most screens
at 1x, while on smaller screens it simply overflows and crops.
